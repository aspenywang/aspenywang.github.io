# Aspen Wang

Minimal personal homepage built with React and Vite.

## Development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

## Deploy

Push to `main`. The GitHub Actions workflow in `.github/workflows/deploy.yml` builds the site and publishes `dist/` to the `gh-pages` branch.
